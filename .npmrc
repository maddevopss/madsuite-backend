# Compatibilité temporaire avec eslint-config-standard@17.1.0.
# Ce paquet exige eslint-plugin-n ^15 || ^16 alors que le dépôt utilise encore ^18.
# À retirer dans une PR dédiée après alignement du graphe ESLint et régénération du lockfile.
legacy-peer-deps=true
